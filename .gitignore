# Virtual environment
.venv/

# Environment variables
.env
.env.*

# Python cache
__pycache__/
*.py[cod]

# VS Code
.vscode/

# Claude Code local settings (per-machine tool permissions, not shared config)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# A/B backtest snapshot cache (local-only fetched data, see docs/AB_TESTING.md)
ab_cache/

# Experiment scratch: analysis scripts, runners and raw per-window results.
# Deliberately untracked -- the condensed verdicts live in docs/ (see README
# "Working with experiments/"). Never the record.
experiments/
